{
  "name": "portfolio-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd my-app && npm install && npm run build && cd ../booking-app && npm install && npm run build && cd ../dashboard && npm install && npm run build && cd ../landing-page/travel-landing-page && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/F-Gebray/FOTO-REPO"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/F-Gebray/FOTO-REPO"
  },
  "homepage": "https://github.com/F-Gebray/FOTO-REPO#readme",
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.3"
  }
}
